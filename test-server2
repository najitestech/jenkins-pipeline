node {
   stage('One') {
       git 'https://github.com/najitestech/jenkins-pipeline.git'
       echo 'Stage one was successful'
   }
   }
   
node {
   stage('Two') {
      sshagent(['najite192.168.0.26']) {
         sh 'uname -a'
         sh 'ls -la'
         sh 'cat /etc/*-release'
   }
   }
   }
   
node {
   stage('Three') {
      sshagent(['najite192.168.0.26']) {
         sh 'ip a'
         sh 'uname -a'
         //sh 'scp -o StrictHostKeyChecking=no /root/public najite@192.168.0.26:/home'
         echo 'Stage three was successful'
   }
   }
   }
