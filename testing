node {
    stage("Build") {
        parallel (
            "First Build" : {
                build("jnks-slave-job2")
            },
            "Second Build" : {
                build("jnks-slave-job3")
            }
        )
        build("test-job")
        parallel (
            "Third Build" : {
                build("jnks-slave-job2")
            },
            "Last Build" : {
                build("jnks-slave-job3")
            }
        )
    }
}
